[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "openbabel"
version = "3.1.1.1"
description = "Meta package pinning openbabel-wheel for this workspace."
requires-python = ">=3.11"
dependencies = [
  "openbabel-wheel==3.1.1.22",
]

[tool.setuptools]
py-modules = []
